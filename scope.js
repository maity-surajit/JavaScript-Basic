/*
if(1===1) {
    //const cool = true; -> through an error as we are printing the cool outside of the scope.
    // let cool = true;
    var cool = true; // > var allow to access value outside of the scope.
}

console.log(cool)
*/

/*

let dog = "Tophy";

function logdog(name) {
    //const dog = name;
    console.log(dog)
}

function go() {
    const dog = "Rockey"
    logdog(dog);
}

go();

*/

/*

if (1 < 0.5) {
    let x = 1;
  } else {
    let x = 2;
  }
  console.log(x); // ReferenceError: x is not defined if let or const use in datatype of x variable.
  
*/




